Class {
	#name : #FilePermissionsDecorator,
	#superclass : #FileOO2Decorator,
	#category : #'O2-FileManager'
}

{ #category : #'menu commands' }
FilePermissionsDecorator >> decorate [

^self permisos.
]
